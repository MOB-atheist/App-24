import React, { component, Component } from 'react';
import './styles.css';

export default class Product extends Component {

    render(){

        return (
            <div>
                PRODUTOS
            </div>
        )
    }
}